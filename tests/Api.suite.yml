actor: ApiTester
suite_namespace: App\Tests\Api
modules:
    enabled:
        - REST:
            url: http://localhost
            depends: Symfony
        - Symfony:
            app_path: 'src'
            environment: 'test'
        - Doctrine:
            depends: Symfony
            cleanup: true
        - \App\Tests\Support\Helper\Api
